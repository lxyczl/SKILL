## Rewritten Text

The model was trained on ImageNet using ResNet-50 architecture. The CNN attained an accuracy of 95%. Optimization was performed using Adam optimizer with a learning rate of 0.001. The LSTM layer comprised 128 hidden units.

## Changes Made

- **Sentence Structure**: Restructured sentence order, used passive voice
- **Vocabulary**: used→using, achieved→attained, used→performed using, had→comprised
- **Preserved Terms**: ResNet-50, ImageNet, CNN, Adam optimizer, learning rate, LSTM (all kept exactly as-is)
- **Voice Changes**: "We used" → "was trained using" (active to passive); "We used" → "Optimization was performed using" (active to passive)

## Similarity Reduction Notes

- Professional terminology preserved: ResNet-50, ImageNet, CNN, Adam optimizer, LSTM
- Technical specifications preserved: "95% accuracy", "128 hidden units", "learning rate of 0.001"
- Sentence structure varied to reduce similarity while maintaining technical accuracy
